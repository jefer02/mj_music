// StorageService is now a lightweight theme/preference store only.
// Audio blobs and song metadata are fully managed by the backend API.
// This file is kept for backward compatibility but no longer used directly.
export {};
